// -- cams
declare namespace NodeJS {
  interface Global {
    camera: {
      outputFolder: string
      thumbs: string
      timeout: string
      maxTimeRecord: string
      videoQuality: string
      fps: string
    }
    url: string
  }
}
