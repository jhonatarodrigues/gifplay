// -- cams
declare namespace NodeJS {
  interface Global {
    camera: {
      outputFolder: string
      timeout: string
      maxTimeRecord: string
      videoQuality: string
      fps: string
    }
  }
}
